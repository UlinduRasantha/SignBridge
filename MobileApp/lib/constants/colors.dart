import 'package:flutter/material.dart';

const backgroundColor = Colors.white;
const primaryColor = Colors.blue;
const accentColor = Colors.blueAccent;
const appbarcolor = Color(0xff004E98);

const texticonColor = Color(0xff004E98);

const boxfilcolor = Color(0xffE3F2FD);
const boxborder = Color(0xff007AFF);

const iconcolour = Color(0xff007AFF);
